class Solution {
  public int search(int[] nums, int target) {
    int lo = 0;
    int hi = nums.length - 1;
    int pivot = pivot(lo, hi, nums);
  }

  int pivot(int lo, int hi, int[] nums) {
    int mid = (lo + hi) / 2;

  }
}