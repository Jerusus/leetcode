class Solution {
  public int[][] outerTrees(int[][] trees) {
    // start with most north east.
    // start with x = 0y + b
    // check for closest slope tree to this slope going clockwise, tie break closest
    // tree in distance.
  }
}